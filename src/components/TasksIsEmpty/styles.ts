import styled from "styled-components";

export const TasksIsEmptyContainer = styled.div`
    display: flex;
    gap: 2rem;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    padding: 4rem 0;
`;